const score = 500 
const balance = new Number(1000)
console.log(balance); 
console.log(balance.toString());
console.log(balance.toString().length);
console.log(balance.toFixed(2));
// to precision is used to make precise value before the decimal places

const cost = 1000000
console.log(cost.toLocaleString('en-IN')); // to localeString is used to use commas in Large numeric values